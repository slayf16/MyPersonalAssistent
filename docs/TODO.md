# Технический долг

## TASK-009 — инварианты и восстановление задачи

2026-09-23 пользователь поручил убрать кнопку «Пауза», остальные незакрытые пункты отложить и завершить работу. Ниже — известные ограничения и недостающие проверки; они не считаются исправленными или пройденными.

- [ ] **New task и активный запрос (V-01 / A-01).** Отмена и ожидание завершения старого provider job, закрытие старого run и защита от позднего ответа. Сейчас новый запрос может ждать mutex старой операции.
- [ ] **Содержимое пользовательских инвариантов (V-03).** Удалить добавленный save-time RU/EN blacklist, оставив технические проверки текста, длины и дубликатов. Правила задаёт пользователь; runtime-проверка ответов на соответствие этим правилам сохраняется. Существующий фильтр может отклонять безопасные правила с `secret`/`token`.
- [ ] **Проверка восстановления ожидания плана (V-02 / A-07).** В cycle 9 runtime `PAUSED` и ручные pause/resume удалены. Проверить восстановление `WAITING_APPROVAL`, в том числе из legacy checkpoint, без автоматического одобрения плана.
- [ ] **Проверка продолжения после закрытия приложения.** В cycle 9 реализована отправка обычного сообщения в сохранённый interrupted этап с новым текстом и контекстом, без специального слова «продолжить» и без запроса при открытии. Проверить полный сценарий закрытия процесса и повторного открытия на target API 36; компиляция не доказывает device E2E. Сохраняется совместимое чтение старых checkpoint с `PAUSED`.
- [ ] **Совместимость существующих JVM-тестов после удаления PAUSED.** Прогон 2026-09-23 остановился на `compileDebugUnitTestKotlin` в `core:agent:api`, `core:agent:impl` и `feature:chat:impl`: тесты ссылаются на удалённые `PAUSED`, `PAUSE/RESUME`, `pause/resume`, `canPause`, `AgentPauseResult`, старые fake engine не реализуют новый lifecycle-контракт. Тестовые файлы не переписывались и suites не отключались; функциональные тесты не выполнены, результат не PASS. Отдельная компиляция приложения успешна. По решению пользователя новые тесты сегодня не писать; адаптацию и недостающее покрытие legacy normalization, continuation по фазам, budgets/approval и late callbacks выполнить позднее.
- [ ] **Противоречивый legacy v3 checkpoint.** Независимое review cycle 9 обнаружило: decoder проверяет `resumeTarget` против phase, но не против старого `pausedFromStatus`. Повреждённая запись `PAUSED`, `PLANNING`, `pausedFromStatus=ACTIVE`, `resumeTarget=RESTORE_WAITING_APPROVAL` принимается как `WAITING_APPROVAL` вместо отклонения. Корректные сохранения старой версии не затронуты. Минимальная правка — проверка совместимости этой пары перед нормализацией. Исправление не выполнено: 2026-09-23 пользователь поручил завершить с TODO, сделать коммит и пуш; review FAIL сохранён как известный долг.
- [ ] **Удалять незавершённый ответ.** Решение пользователя 2026-09-23: если ответ не был отдан до конца, просто удалять его. Реализовать удаление именно незавершённого ответа ассистента, не истории чата или задачи. Сейчас это только TODO: существующее восстановление/повтор прерванного этапа этой записью не изменено.
- [ ] **Repository-тесты (V-04 / A-10).** Расширить покрытия границ и лимитов, delete/tombstone/order, stale/double/failed save, snapshot digest/corruption, affected runs, cancellation/rollback.
- [ ] **Room-тесты (V-05 / A-11).** На target API 36 дополнить migration/reopen oracle: tombstone, audit, snapshot linkage/digest и полный откат состояния при ошибке. Уже выполненные 5 тестов не доказывают всю расширенную матрицу. API 26 не требуется.
- [ ] **Ручная проверка UI.** Завершить accessibility, крупный шрифт/IME, rotation и process-death проверки со скриншотами на target API 36.

Работу над этим списком выполнять отдельным последующим поручением. Отложенное покрытие не выдавать за PASS; успешную компиляцию отличать от функционального тестирования.
